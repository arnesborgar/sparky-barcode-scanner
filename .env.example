# SparkyFitness Barcode Scanner — Configuration
# Copy this file to .env and fill in your values.

# SparkyFitness server URL (no trailing slash)
SPARKY_URL=http://your-server-ip:3010

# API key (generate in SparkyFitness → Settings → API Keys)
# Needs the "diary" permission
SPARKY_API_KEY=

# Meal type is auto-detected by time of day.
# Customize the time windows (24h format HH:MM-HH:MM):
# BREAKFAST_WINDOW=05:00-10:00
# LUNCH_WINDOW=11:00-13:00
# DINNER_WINDOW=14:00-16:00
# Anything outside these windows → Snack

# (Optional) USDA fallback — provider ID from SparkyFitness Settings → Integrations
# When set, incomplete OFF data is enriched from USDA FoodData Central.
# USDA_PROVIDER_ID=

# (Optional) ESPHome kitchen scale REST URL
# When set, the script reads weight before logging. Without it, logs 100g default.
# SCALE_URL=http://10.1.1.x/sensor/weight

# (Optional) Scanner input device path — auto-detected if blank
# Run `python3 -m evdev.evtest` to find your scanner's /dev/input/eventN
# SCANNER_DEVICE=/dev/input/event0
